import CourseLevelPage from '../components/CourseLevelPage';
import { CERTIFICATE_LEVEL } from '../data/coursesData';

export default function CertificateLevelPage() {
  return <CourseLevelPage level={CERTIFICATE_LEVEL} />;
}
