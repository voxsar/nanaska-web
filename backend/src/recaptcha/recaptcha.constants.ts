/** Body field the frontend uses to carry the reCAPTCHA v3 token. */
export const RECAPTCHA_TOKEN_FIELD = 'recaptchaToken';

/** Metadata key holding the expected reCAPTCHA action for a route. */
export const RECAPTCHA_ACTION = 'recaptcha:action';

/** Google's token verification endpoint. */
export const RECAPTCHA_VERIFY_URL = 'https://www.google.com/recaptcha/api/siteverify';
